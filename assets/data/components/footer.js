import {address, email, headerString, phone} from "../global";

const FooterData = {
    left: {
        header: headerString,
        address: address,
    },
    right: {
        email: email,
        phone: phone,
    }
}

export default FooterData
