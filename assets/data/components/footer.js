import {fullName} from "../global";

const FooterData = {
    left: {
        header: fullName,
    },
    right: {
        email: "Email:",
        phone: "Mobile:",
    }
}

export default FooterData
